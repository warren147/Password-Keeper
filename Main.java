import javax.swing.JOptionPane;

public class Main {

	public static void main(String[] args) {
		new PasswordKeeper();
	}
	/*for (int i = 0; i<numofUsers.length; i++) {
		if(numofUsers[i][0] != null){
		if(numofUsers[i][0].equals(usernameInput.getText()) && numofUsers[i][1].equals(passwordInput.getText())){
			JOptionPane.showMessageDialog(null, "Login successful");
			frame2.setVisible(true);
			success = true;
		}
	}
	}
	if (!success) {
		JOptionPane.showMessageDialog(null, "Incorrect username or password or create a new account.");
	}*/
}
